// Automatic speech-based flag classification was removed — it wasn't reliable
// enough. Director flags are now set manually via the Retake / Stumble / Skip
// buttons in the Notes panel. This module is intentionally left empty.
export {}
